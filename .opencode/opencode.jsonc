{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "coolify": {
      "type": "remote",
      "enabled": true,
      "url": "https://host.selfagency.dev/mcp",
      "headers": {
        "Authorization": "Bearer {env:COOLIFY_HOST_TOKEN}"
      }
    },
    "posthog": {
      "type": "remote",
      "enabled": true,
      "url": "https://mcp.posthog.com/mcp"
    },
    "pocketbase": {
      "type": "local",
      "enabled": true,
      "command": ["npx", "-y", "gaspechak-pocketbase-mcp"],
      "environment": {
        // Load credentials from environment (set in .env.local, never committed)
        "PB_URL": "${PB_URL}",
        "PB_EMAIL": "${PB_EMAIL}",
        "PB_PASSWORD": "${PB_PASSWORD}"
      }
    },
    "vite": {
      "type": "remote",
      "url": "http://localhost:5173/__mcp/sse",
      "enabled": true
    }
  }
}
