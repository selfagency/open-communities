<script lang="ts">
// No-op stub for sveltekit-superforms/SuperDebug.svelte in browser tests.
// The real component imports $app/state which is unresolvable in the
// browser runner. SuperDebug only renders in dev mode, so a no-op is safe.
const _noop = null;
</script>
